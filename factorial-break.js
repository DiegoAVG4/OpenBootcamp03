// Este archivo debe calcular el factorial de 10 utilizando un bucle while, una bifurcación if y una sentencia break.
let whibreak = 10;
    let factor = whibreak;
    if (whibreak === 0 || whibreak === 1) 
      console.log(1); 
    while (whibreak > 1) { 
      whibreak--;
      factor *= whibreak;
    } if (whibreak === 11);
    console.log (factor);